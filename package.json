{
  "name": "crimeapp-security-rules",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --config firebase.rules-test.json --only firestore,storage \"node --test firebase/rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
